[package]
name = "vecGradient"
version = "0.1.0"
edition = "2021"
license = "PolyForm-Noncommercial-1.0.0"
description = "vecGradient: the canonical Vector15D type of the Gradient Codec — binds affective telemetry to text, weight/temperature/axis/magnetic poles preserved"

[dependencies]
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"
thiserror = "1.0"

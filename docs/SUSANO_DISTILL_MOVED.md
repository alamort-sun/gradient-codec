# Susano distill moved

The Grok-edge distillation that lived at `docs/susano-distill/` (`f9f7608`) now homes in **[gradient-jelle](https://github.com/alamort-sun/gradient-jelle)** at `docs/susano-distill/`.

Reason: jelle is the JEPA + LLM + MoE plane that routes and storms against those priors. Codec remains geometry authority (`vecGradient::Vector15D`).

History in this repo: `git show f9f7608:docs/susano-distill/`.
